import 'package:flutter/material.dart';

class HightintsScreen extends StatelessWidget {
  const HightintsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hightinits'),
      ),
    );
  }
}
